const doubleIt = num => num * 2;
const add = (x, y) => x + y;
const give5 = () => 5;
const doMath = (x, y) =>{
    const sum = x + y;
    cosnt diff = x - y;
    const result = sum * diff;
    return result;
}